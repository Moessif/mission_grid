目录
七、硬件驱动及使用3
7.1 Unitree L2激光雷达3
7.1.1 Unitree L2介绍3
7.1.2 Unitree L2相关SDK及ROS包安装6
7.1.3 Unitree L2的参数配置及启动6
7.1.4 Unitree L2数据的应用10
7.1.5 Unitree L2补充说明13
7.2 Livox Mid-360激光雷达14
7.2.1 Livox Mid-360介绍14
7.2.2 Livox Mid-360相关SDK及ROS包安装16
7.2.3 Livox Mid-360的参数配置及启动16
7.2.4 Livox Mid-360数据的应用26
7.2.5 Livox Mid-360补充说明28
7.3 Realsens D435深度相机29
7.3.1 Realsens D435介绍29
7.3.2 Realsens D435相关SDK及ROS包安装29
7.3.3 Realsens D435的参数配置及启动29
7.3.4 Realsens D435数据的应用41
7.3.5 Realsens D435补充说明43
7.4 空循环LinkTrack UWB44
7.4.1 LinkTrack UWB介绍44
7.4.2 LinkTrack UWB相关SDK及ROS包安装44
7.4.3 LinkTrack UWB的参数配置及启动44
7.4.4 LinkTrack UWB数据的应用44
7.4.5 LinkTrack UWB补充说明44
7.5 USB相机45
7.5.1 USB相机介绍45
7.5.2 USB相机的使用45
7.5.3 USB相机的内参标定54
7.5.4 USB相机补充说明64
七、硬件驱动及使用
本章节程序均在ros环境下运行，测试程序位于sensor_examples功能包内
7.1 Unitree L2激光雷达
7.1.1 Unitree L2介绍
1. 硬件参数
型号
L2
激光波长
905nm
人眼安全等级[1]
Class 1(IEC60825-1:2014) 人眼安全
最大量程[2]
30M（@90% 反射率）15M（@10% 反射率）
近处盲区[3]
0.05m
FOV
360°* 90°/360°* 90°(负角度模式)
采样频率
128000 点 / 秒
有效频率
64000 点 / 秒
扫描方式
无接触无刷转镜扫描
4D 信息
3D 位置 +1D 灰度(支持2D模式)[4]
周向扫描频率
5.55Hz(可定制调节)[5]
竖直扫描频率
216Hz
通讯接口
ENET UDP和TTL UART
通信波特率
4000000 bps(TTL UART)
测量精度[6]
≤2.0cm
角分辨率
0.64°
测距分辨率
4.5mm
IMU采样率
1KHz
IMU上报频率
500Hz
姿态感知维度
3 轴加速度 +3 轴陀螺仪
LED 灯环分辨率
60°
LED灯环刷新率
5.55Hz
抗强光能力
100Klux
工作环境温度[7]
-10℃~50℃
存储环境温度
-20℃~60℃
防护等级[8]
IP54
功率[9]
10W（环境温度 25℃）
工作电压
12V DC
尺寸
75（宽）x75（深）x65（高）mm
重量
230g
[1] 激光器的瞬间峰值功率为 25W，实际使用时平均功率将远低于此值，并采用脉冲方式驱动仅在极短的时间内进行发射动作，从而确保对人类及宠物的安全性，达到 Class I 级别的激光器安全标准。[2] 此处仅展示了反射率典型值，实际值取决于环境条件及目标物体特性。[3] 若目标物体距离 0.05m 时，激光探测测距仪可探测并输出点云数据，但由于无法保证探测精度，此数据仅供参考。[4] 2D 模式下角度范围为 180°或者 192°（负角度模式），有效频率仍为 64000 点 / 秒。[5] 定制周向扫描频率对采购数量有一定要求。L2下电机转速的定制范围为4~10Hz，同时需注意：转速越高震动和噪声越大，动态性能越好，不容易停转，更适合应用在动态应用场景中；转速越低震动和噪声越小，动态性能越差，容易停转，更适合应用在类似监控这种静态应用场景中；转速高低不影响点云数据量，只要安装牢固，对雷达性能影响可忽略；转速变化影响功耗，参数表中的功耗在5.55HZ下测得。[6] 为保证对量程内不同反射率物体的有效探测，个别位置可能会出现点云精度轻微下降的现象。测试条件为环境温度 25℃，目标物体反射率 90%，测试距离 15m。[7] 在高温和低温环境、强烈振动、大雾天气等环境下，L2 性能将有小幅下降。此外，长期高温工作可能会影响产品性能甚至造成产品损坏。建议用户增加散热措施，保证底盖温度不超过85℃。若温度过高将会触发过温保护机制，L2 会发出超温警告，严重超温时 L2 将停止运行。[8] 在不同安装⻆度下，L2 的防护效果差异较大，请根据实际安装⻆度， 自行增加外部防护 ; 由于安装或自行外部防护不当导致的损坏，不在保修范围之内。[9] 不同环境下的稳定功率和峰值功率不同 , 当环境温度范围为 -10℃至 30℃时，L2 将自动运行自加热模式，待温度满足要求后才会出点云，且此时峰值功率可达 13W，请合理设计供电电源，保证设备正常工作。
2. 连接方法
通过串口ttyS0进行连接
3. 作用
在本设备中，Unitree L2激光雷达主要用于SLAM定位。通过与IMU数据融合输出定位信息。
4.坐标系
激光雷达的坐标系定义如下图所示，它满足右手坐标系的定义。
激光雷达点云坐标系的原点位于激光雷达底部安装面的中心，其+X轴与底部线缆出线方向相反，其+Y轴是通过将+X轴逆时针旋转90度得到的，其+Z轴垂直底面向上。
7.1.2 Unitree L2相关SDK及ROS包安装
出场设备均已安装Unitree L2相关SDK及ROS包
7.1.3 Unitree L2的参数配置及启动
1. 找到Unitree L2的ROS驱动包和相关配置文件
在终端输入rospack find unitree_lidar_ros，使用rospack find来寻找功能包unitree_lidar_ros的路径，如图所示功能包在
/home/orangepi/Unitree_L2/unilidar_sdk2-2.0.4/unitree_lidar_ros/src目录下。
设备不同，路径可能不一样。
rospack find unitree_lidar_ros
如果显示“[rospack] Error: package 'pkg_mane' not found”，可能是没有source环境导致的，可以换成以下指令，将my_robot_pkg替换为要寻找的功能包名，然后比照上图找到正确的路径。
find /opt/ros/*/share ~/ -type d -name "my_robot_pkg" 2>/dev/null
找到功能包中，config文件夹下的config.yaml文件，这个文件就是Unitree L2激光雷达的ROS驱动包的配置文件。
2. 配置文件参数
注：未提及的其他参数，除非完全理解，否则不建议更改。
（1）initialize_type
含义：雷达的连接方式
说明：1--串口连接；2--UDP网络连接
（2）work_mode
含义：雷达的工作模式
说明：工作模式的设置是通过一个的uint32_t整型变量实现，其每一个bit位对应一个功能的切换。按照Bit位置从低到高，每一位0或1对应的功能如下表所示：
比特位
功能
取值0
取值1
0
切换标准FOV和广角FOV
标准视野（180°）
广角视野（192°）
1
切换3D和2D测量模式
3D测量模式
2D测量模式
2
使能或关闭IMU
使能IMU
关闭IMU
3
切换网口模式和串口模式
网口模式
串口模式
4
切换激光雷达上电默认启动模式
上电即自行启动
上电保持不转动并等待启动命令
5-31
保留
保留
保留
常用的使用模式为标准FOV+3D测量+使能IMU+上电自启动，即这几个bit位置均保持为0即可，我们只需要确定是使用网口连接模式还是串口连接模式。
如果使用网口连接模式，则将工作模式配置为0（即整型变量的所有bit位置均等于0，二进制数为0000 0000 0000 0000 0000 0000 0000 0000）
如果使用串口连接模式，则将工作模式配置为8（即整型变量的第3个bit位置设为1，其余位的bit位置均设为0，二进制数为0000 0000 0000 0000 0000 0000 0000 1000）
（3）use_system_timestamp
含义：是否使用 主机系统时间戳（而不是 LiDAR 自带的时间戳）作为点云/IMU 消息的时间
说明：一般不用改
（4）range_min和range_max
含义：雷达点云的有效距离范围（m）
说明：会自动过滤掉小于 range_min 或大于 range_max 的点
（5）cloud_scan_num
含义：每帧点云包含的 扫描线数
说明：对于机械雷达，这个值应匹配硬件
（6）serial_port
含义：连接的 串口设备路径
说明：本设备使用的串口一般为ttyS0
（7）baudrate
含义：串口波特率
说明：必须与 LiDAR 固件设置一致，否则无法通信
3. 启动功能包
在找到config配置文件，了解并配置了相关参数后，我们就可以来启动了。
我们在终端使用roslaunch指令启动run_without_rviz.launch文件，完成启动。
roslaunch unitree_lidar_ros run_without_rviz.launch
接着新开一个终端，使用rostopic list指令查看当前话题。
rostopic list
使用rostopic hz /unilidar/cloud和rostopic echo /unilidar/cloud这两个指令查看雷达点云话题/livox/lidar的频率和数据。
rostopic hz /unilidar/cloud
rostopic echo /unilidar/cloud
此时，我们就完成了从插入硬件到使用ROS驱动发布Unitree L2点云数据的全部步骤，接下来就可以根据需要来使用话题数据了。
7.1.4 Unitree L2数据的应用
要应用传感器数据，首先需要获取到传感器数据，因此，提供一个简单的数据获取示例代码。
在sensor_examples功能包scripts目录下创建代码文件L2_data.py。
编写代码，通过以下代码，我们简单的完成了ros节点的创建，订阅者的注册，订阅到数据后回调函数的具体处理，也就是通过sensor_msgs.point_cloud2.read_points（）函数将订阅到的PointCloud2数据进行解析并打印部分结果（解析出来的每个点的xyz等数据）。我们也可以自己通过fields属性内对点云数据的定义，自己从data解析点云。
首先驱动雷达
roslaunch unitree_lidar_ros run_without_rviz.launch
接着启动示例代码
rosrun sensor_examples L2_data.py
激光雷达的坐标系定义如下图所示，它满足右手坐标系的定义。
激光雷达点云坐标系的原点位于激光雷达底部安装面的中心，其+X轴与底部线缆出线方向相反，其+Y轴是通过将+X轴逆时针旋转90度得到的，其+Z轴垂直底面向上。
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 导入ROS的Python客户端库
import rospy
# 导入点云消息类型
from sensor_msgs.msg import PointCloud2
# 导入用于解析PointCloud2数据的工具函数
import sensor_msgs.point_cloud2 as pc2
def cloud_callback(msg):
"""
回调函数，处理来自激光雷达的点云数据
:param msg: 接收到的sensor_msgs/PointCloud2类型消息
"""
# 打印接收到的点云总点数（height * width）
rospy.loginfo("Received a point cloud with %d points", msg.width * msg.height)
# 定义需要解析的字段名称列表，顺序需与点云中fields定义一致
field_names = ["x", "y", "z", "intensity", "ring", "time"]
# 使用pc2.read_points读取指定字段的数据，并跳过包含NaN的无效点
points = pc2.read_points(msg, field_names=field_names, skip_nans=True)
# 初始化计数器，最多打印5个点
count = 0
max_print = 5
# 遍历解析出的每个点
for point in points:
# 解包每个字段：x, y, z为坐标，intensity为反射强度，ring为激光线号，time为点级时间戳（相对偏移）
x, y, z, intensity, ring, t = point
# 打印当前点的详细信息
rospy.loginfo(
"Point %d: x=%.3f, y=%.3f, z=%.3f, intensity=%.1f, ring=%d, time=%.6f",
count, x, y, z, intensity, ring, t
)
# 计数器递增
count += 1
# 达到最大打印数量后退出循环
if count >= max_print:
break
rospy.loginfo("--- End of sample points ---\n")
def main():
# 初始化的ROS节点
rospy.init_node('L2_subscriber')
# 创建订阅者
rospy.Subscriber("/unilidar/cloud", PointCloud2, cloud_callback)
# 打印提示信息
rospy.loginfo("Subscribing to /unilidar/cloud ...")
# 保持节点活跃
rospy.spin()
if __name__ == '__main__':
try:
main()
except rospy.ROSInterruptException:
pass
7.1.5 Unitree L2补充说明
无
7.2 Livox Mid-360激光雷达
7.2.1 Livox Mid-360介绍
1. 硬件参数
型号
MID-360
激光波长
905nm
人眼安全级别¹
Class 1(IEC60825-1:2014) 人眼安全
量程 (@ 100 klx)
40 m @ 10% 反射率70 m @ 80% 反射率
近处盲区 ²
0.1 m
FOV
水平 360°, 竖直 -7°~52°
测距随机误差³ (1σ)
≤ 2 cm ⁴ (@ 10m)
≤ 3 cm ⁵ (@ 0.2m)
角度随机误差（1σs）
< 0.15º
点云输出
200,000 点/秒 (可配置第一回波)
点云帧率
10 Hz (典型值)
数据网口
100 BASE-TX 以太网
数据同步方式
IEEE 1588-2008 (PTPv2), GPS
抗串扰功能
有
虚警率 (@ 100 klx) ⁶
< 0.01%
IMU
内置 IMU 型号：ICM40609
工作环境温度⁷
-20℃ 至 55℃
防护等级
IP67
功率 ⁸
6.5 W
供电电压范围
9 ~ 27 V DC
尺寸
65×65×60 mm
重量
265 g
[1] 激光探测测距仪中内置激光器辐射光的发散角典型值为 25.2°（水平）× 8°（竖直）（半高宽， FWHM），最大激光功率可能超过 70 W。切勿拆开 Mid-360，以免造成危险。[2] 若目标物体距离 0.1 m 至 0.2 m，激光探测测距仪可探测并输出点云数据，但由于无法保证探测精度，此数据仅供参考。[3] 为保证对量程内不同反射率物体的有效探测，个别位置可能会出现点云精度轻微下降的现象。[4] 测试条件为环境温度 25℃，目标物体反射率 80%，测试距离 10 m。[5] 测试条件为环境温度 25℃，目标物体反射率 80%，测试距离 0.2 m。在 0.1~1 m 范围内的低反射率物体和细小物体（包括但不限于黑色泡棉、水面、亮面或经消光处理的物体、细线等），可能无法保证探测效果。[6] 在 100 klx 日光条件下，环境温度 25℃，环境杂散光产生的虚警噪点的比例。[7] 在高温和低温环境、强烈振动、大雾天气等环境下， Mid-360 性能将有小幅下降。此外，长期高温工作可能会影响产品性能，甚至造成产品损坏。建议用户增加散热措施，保证外壳温度不超过 80℃。若温度过高将会触发过温保护机制， Mid-360 会发出超温警告，严重超温时 Mid-360 将停止运行。[8] 当环境温度范围为 -20℃至 0℃时， Mid-360 将自动运行自加热模式，峰值功率可高达 14W，请合理设计供电电源，保证设备正常工作。
2. 连接方法
通过网口进行连接
在终端输入ip link show eth0查看设备是否正常连接，存在LOWER_UP表示物理链路已建立。
ip link show eth0
3. 作用
在本设备中，Mid-360激光雷达主要用于SLAM定位。通过与IMU数据融合输出定位信息。
4.坐标系
Mid-360 的直角坐标O-XYZ的定义如下图所示。点O为原点，O-XYZ为 Mid-360的点云坐标系。，X向前，Y向左，Z向上的右手坐标系。
7.2.2 Livox Mid-360相关SDK及ROS包安装
出场设备均已安装Livox Mid-360相关SDK及ROS包
7.2.3 Livox Mid-360的参数配置及启动
1. 配置静态IP
在将Mid-360激光雷达连接到机载电脑后，首先第一步，就是配置机载电脑有线连接的静态IP，从而使得激光雷达和机载电脑能正常通信，为获取激光雷达数据做准备。
在这里我们以Jetson Orin NX为例，来进行静态IP配置，一般情况下，无人机出厂已进行相关配置。
（1）通过远程桌面配置（二选一）
首先使用NoMachine完成设备连接，点击右上角，点击设置
接着点击网络-有线的设置
接着点击IPv4，在IPv4方式选择手动，激光雷达的IP地址为192.168.1.1XX，子网掩码为 255.255.255.0，默认网关为 192.168.1.1。因为激光雷达和机载电脑要处于同一网段下才能通信，因此，我们在地址填入同样网段的地址192.168.1.5，子网掩码255.255.255.0，网关192.168.1.1，填入完毕后，点击右上角的应用。此时静态IP就配置完毕了。
（2）通过终端命令行配置（二选一）
除了在远程桌面配置静态IP以外，也可以通过命令行终端来进行配置。
首先使用cd跳转到网络IP配置的目录，接着使用ls查看目录下文件，可以看到有一个名为“有线连接 1.nmconnection”的文件。
cd /etc/NetworkManager/system-connections/
ls
找到文件后，使用vim打开文件。
sudo vim 有线连接\ 1.nmconnection
激光雷达的IP地址为192.168.1.1XX，子网掩码为 255.255.255.0，默认网关为 192.168.1.1。因为激光雷达和机载电脑要处于同一网段下才能通信，因此，我们需要填入同样网段的地址192.168.1.5，子网掩码255.255.255.0，网关192.168.1.1。所以我们在图示所框选的位置填入地址，子网掩码和网关。
修改完配置文件后，退出保存，reboot重启机载电脑即可。
sudo reboot
（3）查看配置是否生效
我们配置好静态IP，并插好雷达上电后，在终端输入ifconfig，查看当前有线连接的IP。发现此时IP为我们设置的IP。
ifconfig
接着，查询激光雷达的IP地址，激光雷达的IP地址格式为192.168.1.1XX，其中XX为Mid-360的SN码最后两位数字。如图所示，最后两位为90，因此，该雷达的IP地址为192.168.1.190。
接着在终端输入ping -c 5 192.168.1.190，使用ping命令测试连接。192.168.1.190替换为自己的雷达IP地址。设备通信正常。
ping -c 5 192.168.1.190
2. Livox ROS Driver
在连接好激光雷达并配置好静态IP后，我们就可以通过Livox ROS Driver将激光雷达的数据通过ROS话题发布出来，供其他ROS节点来使用。在启动ROS驱动之前，我们需要进行相应的参数配置。
（1）找到激光雷达ROS驱动包和相关配置文件
在终端输入rospack find livox_ros_driver2，使用rospack find来寻找功能包的路径，如图所示功能包在/home/zmofly/livox_ws/src目录下。设备不同，路径可能不一样。
rospack find livox_ros_driver2
如果显示“[rospack] Error: package 'pkg_mane' not found”，可能是没有source环境导致的，可以换成以下指令，将my_robot_pkg替换为要寻找的功能包名，然后比照上图找到正确的路径。
find /opt/ros/*/share ~/ -type d -name "my_robot_pkg" 2>/dev/null
找到功能包中，launch_ROS1文件夹下的msg_MID360.launch文件和config文件夹下的MID360_config.json文件，这两个文件就是激光雷达ROS驱动包的启动文件和激光雷达的配置文件。
（2）配置文件参数
找到了配置文件后，我们就要根据具体需要去进行参数的配置。首先是msg_MID360.launch文件的部分参数说明。
注：未提及的其他参数，除非完全理解，否则不建议更改。
msg_MID360.launch文件部分参数解析:
（1）publish_freq
含义：设置点云发布频率
默认值：10.0
说明：浮点数据类型，推荐值为5.0、10.0、20.0、50.0等。最大发布频率为100.0 Hz。频率一般设为10。频率太高会导致点云稀疏，影响slam定位的效果
（2）multi_topic
含义：LiDAR设备是否通过独立的主题来发布点云数据
默认值：0
说明：决定雷达数据是要都发到同一个话题上，还是每个雷达都有一个独立的话题。
0--所有LiDAR设备都使用相同的主题发布点云数据
1--每个LiDAR设备都有自己的主题来发布点云数据
（3）xfer_format
含义：设置点云格式
默认值：0
说明：
可发布的点云格式有以下三种，请根据算法或应用需求自行选择。
0 -- Livox pointcloud2（PointXYZRTLT）点云格式，如下所示。
数据类型
字段名
说明
float32
x
点的X轴坐标，单位（m）
float32
y
点的Y轴坐标，单位（m）
float32
z
点的Z轴坐标，单位（m）
float32
intensity
激光点的反射率，范围从0.0到255.0
uint8
tag
激光点的标签
uint8
line
激光雷达中的激光线编号
float64
timestamp
激光点的时间戳
1 -- Livox自定义点云格式，如下所示。
数据类型
字段名
说明
std_msgs/Header
header
ROS标准消息头
uint64
timebase
第一个点的时间戳
uint32
point_num
点云总数
uint8
lidar_id
激光雷达设备ID编号
uint8[3]
rsvd
保留字段
CustomPoint[]
points
点云数据
上述定制数据包中的自定义点云（CustomPoint）格式如下所示：
数据类型
字段名
说明
uint32
offset_time
相对于timebase的偏移时间
float32
x
点的X轴坐标，单位（m）
float32
y
点的Y轴坐标，单位（m）
float32
z
点的Z轴坐标，单位（m）
uint8
reflectivity
激光点的反射率，范围从0到255
uint8
tag
激光点的标签
uint8
line
激光雷达中的激光线编号
2 -- 标准pointcloud2（pcl：：PointXYZI）点云格式，位于 PCL 库中（仅用于 ROS）
在了解了激光雷达ROS驱动包的启动文件的部分参数后，接着是MID360_config.json激光雷达的配置文件的参数解析。
激光雷达配置（如IP、端口、数据类型等）可以通过JSON风格的配置文件设置。单个HAP、Mid360和混合激光雷达的配置文件都在“config”文件夹里。启动文件msg_MID360.launch中命名为“user_config_path”的参数表示该json文件路径。
一般来说，只需要修改配置文件的IP地址即可，其余参数不建议修改。如下图所示，将“cmd_data_ip”，“push_msg_ip”，“point_data_ip”，“imu_data_ip”这四个参数的IP修改为之前我们配置的静态IP。“ip”这个参数修改为激光雷达的IP即可。
（3）启动功能包
我们已经完成了机载电脑静态IP的配置，以及ROS驱动包两个参数文件的了解和配置。接着就可以进行启动了。
我们在终端使用roslaunch指令启动msg_MID360.launch文件，并加载MID360_config.json参数文件。完成启动。
roslaunch livox_ros_driver2 msg_MID360.launch
接着新开一个终端，使用rostopic list指令查看当前话题。
rostopic list
使用rostopic hz /livox/lidar和rostopic echo /livox/lidar这两个指令查看雷达点云话题/livox/lidar的频率和数据。
rostopic hz /livox/lidar
rostopic echo /livox/lidar
此时，我们就完成了从插入硬件到使用ROS驱动发布Mid-360点云数据的全部步骤，接下来就可以根据需要来使用话题数据了。
7.2.4 Livox Mid-360数据的应用
要应用传感器数据，首先需要获取到传感器数据，因此，提供一个简单的数据获取示例代码。
在sensor_examples功能包scripts目录下创建代码文件mid-360_data.py。
编写代码，通过以下代码，我们简单的完成了ros节点的创建，订阅者的注册，订阅到数据后回调函数的具体处理，也就是将订阅到的数据（包括一帧数据所包含的点的xyz坐标）进行打印，通过简单的“.”就可以获取帧所搭载的具体数据。
首先驱动雷达
roslaunch livox_ros_driver2 msg_MID360.launch
接着启动示例代码
rosrun sensor_examples mid-360_data.py
通过这些具体的数据，我们除了进行简单的打印，也可以做其他事情，比如通过点的xyz坐标，来判断激光雷达到某个方位障碍物的距离，从而实现简单的障碍物感知等。
Mid-360 的直角坐标O-XYZ的定义如下图所示。点O为原点，O-XYZ为 Mid-360的点云坐标系。，X向前，Y向左，Z向上的右手坐标系。
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 导入ros的python接口
import rospy
# 导入livox自定义的ustomMsg数据类型
from livox_ros_driver2.msg import CustomMsg
# 回调函数
def mid360_callback(msg):
# 打印帧基本信息
rospy.loginfo("=== New Livox Frame ===")
rospy.loginfo(f"Frame timebase: {msg.timebase} ns") # 纳秒 ns
rospy.loginfo(f"Point count: {msg.point_num}")
rospy.loginfo(f"Lidar ID: {msg.lidar_id}")
# 遍历前5个点
for i, pt in enumerate(msg.points[:5]):
# 计算绝对时间（纳秒）
abs_time_ns = msg.timebase + pt.offset_time * 1000  # offset_time单位是微秒 ns, 需×1000转为纳秒 ns
rospy.loginfo(
f"Point {i}: "
f"x={pt.x:.3f}, y={pt.y:.3f}, z={pt.z:.3f}, "
f"ref={pt.reflectivity}, tag={pt.tag}, line={pt.line}, "
f"t_rel={pt.offset_time} μs, t_abs={abs_time_ns} ns"
)
def main():
# 初始化ros节点
rospy.init_node('mid360_subscriber')
# 创建订阅者
rospy.Subscriber('/livox/lidar', CustomMsg, mid360_callback)
rospy.spin()
if __name__ == '__main__':
try:
main()
except rospy.ROSInterruptException:
pass
7.2.5 Livox Mid-360补充说明
无
7.3 Realsens D435深度相机
7.3.1 Realsens D435介绍
1. 硬件参数
2. 连接方法
通过USB插口进行连接
在终端输入lsusb查看设备是否正常连接
lsusb
3. 作用
彩色图像可用途图像识别追踪等处理，双目灰度图像可用于视觉SLAM定位，深度图像可用于建图。
7.3.2 Realsens D435相关SDK及ROS包安装
出场设备均已安装Realsens D435相关SDK及ROS包
7.3.3 Realsens D435的参数配置及启动
1. 找到Realsens D435的ROS驱动包和相关配置文件
在终端输入rospack find realsense2_camera，使用rospack find来寻找功能包realsense2_camera的路径，如图所示功能包在~/tools_ws/src/realsense-ros/目录下。设备不同，路径可能不一样。
rospack find realsense2_camera
如果显示“[rospack] Error: package 'pkg_mane' not found”，可能是没有source环境导致的，可以换成以下指令，将my_robot_pkg替换为要寻找的功能包名，然后比照上图找到正确的路径。
find /opt/ros/*/share ~/ -type d -name "my_robot_pkg" 2>/dev/null
找到功能包中，launch文件夹下的rs_camera.launch文件，这个文件就是Realsens D435深度相机的ROS驱动包的launch启动文件。
2. 配置文件参数
找到了配置文件后，我们就要根据具体需要去进行参数的配置。
注：未提及的其他参数，除非完全理解，否则不建议更改。
（1）设备序列号参数
参数名
含义
serial_no
相机序列号（唯一标识），用于多设备区分，单设备可留空
说明：该参数为相机的序列号，每台RealSense出厂时烧录，全球唯一。在使用多台设备时进行设备指定和区分。如果只有一台设备，则可以留空不填。
在终端输入rs-enumerate-devices可查看设备的序列号。
rs-enumerate-devices
（2）流配置参数
参数名
含义
depth_width
深度图像的宽度
depth_height
深度图像的高度
enable_depth
是否启用深度图像
depth_fps
深度图像的帧率
infra_width
红外图像的宽度
infra_height
红外图像的高度
enable_infra
是否启用红外图像
enable_infra1
是否启用左红外图像
enable_infra2
是否启用右红外图像
infra_fps
红外图像的帧率
color_width
彩色图像的宽度
color_height
彩色图像的高度
enable_color
是否启用彩色图像
color_fps
彩色图像的帧率
说明：以上参数均为相机视频流的配置参数，是否发布，图像大小，图像帧率。在终端输入rs-enumerate-devices可查看每种视频流支持的图像大小和帧率。
rs-enumerate-devices
（3）其他功能参数
参数名
含义
enable_sync
启用帧同步，用于多传感器时间戳对齐
align_depth
将深度图像对齐到彩色图像
说明：align_depth参数启用后，会发布一个名为
/camera/aligned_depth_to_color/image_raw的新话题，该话题发布对齐后的深度图。
彩色图像和未对齐的深度图像如下图所示。可以看到，深度图像中的轮廓明显与彩色图像对不上。
对齐后的深度图像如下图所示。可以看到，深度图像中的轮廓基本与彩色图像对上了。
（4）额外添加的参数
D435还有一个参数可能会用到，那就是红外发射器的开关，红外发射器可以优化深度图像的效果，但同时也会影响双目灰度图像的成像效果。因此，可以在launch文件内添加以下代码来控制红外发射器的开关。
<arg name="emitter_enable"            default="false"/>
<!-- rosparam set /camera/stereo_module/emitter_enabled false -->
<rosparam>
/camera/stereo_module/emitter_enabled: 0
</rosparam>
<rosparam if="$(arg emitter_enable)">
/camera/stereo_module/emitter_enabled: 1
</rosparam>
当default为true时，开启红外发射器，效果如下图。
当default为false时，关闭红外发射器，效果如下图。
3. 启动功能包
在找到启动launch文件，了解并配置了相关参数后，我们就可以来启动了。
我们在终端使用roslaunch指令启动rs_camera.launch文件，完成启动。
roslaunch realsense2_camera rs_camera.launch
在启动后，终端会输出一些信息，我们来了解一下输出的含义。
（1）版本信息
表明了的 realsense2_camera ROS 包版本是 v2.3.2，编译和运行时链接的 librealsense2 SDK 版本都是 v2.55.1。保证 SDK 和 ROS 驱动版本匹配。
https://github.com/IntelRealSense/realsense-ros/releases
[ INFO] RealSense ROS v2.3.2
[ INFO] Built with LibRealSense v2.55.1
[ INFO] Running with LibRealSense v2.55.1
（2）设备的发现与识别
# 识别到设备序列号 310222077382
[ INFO] Device with serial number 310222077382 was found.
# 设备物理端口为
/sys/devices/platform/3610000.xhci/usb2/2-1/2-1:1.0/video4linux/video0
[ INFO] Device physical port:
/sys/devices/platform/3610000.xhci/usb2/2-1/2-1:1.0/video4linux/video0
# 识别到的设备名为Intel RealSense D435
[ INFO] Device with name Intel RealSense D435 was found.
# USB 端口为 2-1
[ INFO] Device with port number 2-1 was found.
#连接在 USB 3.2 总线上
[ INFO] Device USB type: 3.2
（3）设备详细信息
# ROS 节点命名空间为 camera
[ INFO] ROS Node Namespace: camera
# 设备名为Intel RealSense D435
[ INFO] Device Name: Intel RealSense D435
# 设备序列号为310222077382
[ INFO] Device Serial No: 310222077382
# 设备物理端口为
/sys/devices/platform/3610000.xhci/usb2/2-1/2-1:1.0/video4linux/video0
[ INFO] Device physical port:
/sys/devices/platform/3610000.xhci/usb2/2-1/2-1:1.0/video4linux/video0
# 固件版本为 5.15.0.2
[ INFO] Device FW version: 5.15.0.2
# 设备产品ID为0x0B07
[ INFO] Device Product ID: 0x0B07
（4）功能开关状态
# 是否启用深度对齐彩色：关
[ INFO] Align Depth: Off
# 是否启用帧同步：关
[ INFO] Sync Mode: Off
（5）传感器检测
[ INFO] Device Sensors:
# 找到左右红外摄像头和激光发射器
[ INFO] Stereo Module was found.
# 找到RGB相机
[ INFO] RGB Camera was found.
（6）流配置确认
# 图像的宽高，帧率，格式
[ INFO] depth stream is enabled - width: 640, height: 480, fps: 30, Format: Z16
[ INFO] infra1 stream is enabled - width: 640, height: 480, fps: 30, Format: Y8
[ INFO] infra2 stream is enabled - width: 640, height: 480, fps: 30, Format: Y8
[ INFO]color stream is enabled - width: 640, height: 480, fps: 30, Format: RGB8
（7）话题发布者的频率设置
# ROS topic发布者已创建，并设置期望频率为 30Hz
[ INFO] setupPublishers...
[ INFO] Expected frequency for depth = 30.00000
[ INFO] Expected frequency for infra1 = 30.00000
[ INFO] Expected frequency for infra1 = 30.00000
[ INFO] Expected frequency for color = 30.00000
（8）流同步与启动完成
# 所有视频流已成功配置
[ INFO] setupStreams...
# 以 Depth流作为主同步基准
[ INFO] SELECTED BASE:Depth, 0
# 节点启动成功
[ INFO] RealSense Node Is Up!
以上输出，一般我们只需要关注SDK和ROS包版本是否匹配，插入的USB接口是否为3.0（2.0传输速率低，可能导致话题无图像发布，可在launch文件中关闭不需要的图像数据来减缓传输压力），图像的分辨率、FPS、格式是否符合设置。其他的数据可根据情况来关注。
4. 话题查看
在完成启动，并且输出符合预期后，接着新开一个终端，使用rostopic list指令查看当前话题。
rostopic list
在一大堆话题中，我们通常仅需关注三类话题即可
（1）相机内参和畸变参数
这些话题会发布相机每种摄像头的内参和畸变参数，很多精确的图像计算都需要订阅这些话题或使用这些数据。
/camera/color/camera_info
/camera/depth/camera_info
/camera/infra1/camera_info
/camera/infra2/camera_info
（2）原始图像数据
未压缩的原始图像，最常用的图像话题。
/camera/color/image_raw
/camera/depth/image_rect_raw
/camera/infra1/image_rect_raw
/camera/infra2/image_rect_raw
（3）压缩后的图像数据
压缩后的图像数据，有损压缩，可能会对图像精度比较敏感的图像处理任务的结果产生影响。
/camera/color/image_raw/compressed
/camera/depth/image_rect_raw/compressed
/camera/infra1/image_rect_raw/compressed
/camera/infra2/image_rect_raw/compressed
7.3.4 Realsens D435数据的应用
一般来说，除了彩色图像以外，双目灰度图像和深度图像自己来拿做处理是相对比较少见的，大部分都是直接使用一些定位建图的算法来订阅处理这些数据。不过依旧在此提供一个简单的示例代码。
在sensor_examples功能包scripts目录下创建代码文件d435_data.py。
编写代码，该示例代码通过订阅彩色图像和对齐后的深度图，获取深度图中心点的深度并打印出来，同时向/camera/color/image_annotated话题发布标注了中心的图像，易于观察。
首先启动Realsens D435，注意要开启图像对齐
roslaunch realsense2_camera rs_camera.launch align_depth:=true
接着启动示例代码
rosrun sensor_examples d435_data.py
之后启动rqt_image_view，选择/camera/color/image_annotated话题，查看图像
rqt_image_view
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 导入ROS的Python接口库
import rospy
# 导入用于同步多个话题消息的工具
import message_filters
# 导入图像消息类型
from sensor_msgs.msg import Image
# 用于在ROS图像和OpenCV图像之间进行转换的桥接器
from cv_bridge import CvBridge
# 导入OpenCV库
import cv2
# 创建一个全局的CvBridge实例，用于转换ROS图像消息和OpenCV图像
bridge = CvBridge()
# 定义一个发布器，用于发布经过标注后的彩色图像
annotated_image_pub = rospy.Publisher('/camera/color/image_annotated', Image, queue_size=1)
def image_callback(color_msg, depth_msg):
"""
回调函数，处理来自相机的彩色和深度图像信息
:param color_msg: 彩色图像的消息
:param depth_msg: 深度图像的消息
"""
try:
# 将ROS图像消息转换为OpenCV格式的图像
color_cv = bridge.imgmsg_to_cv2(color_msg, "bgr8")
depth_cv = bridge.imgmsg_to_cv2(depth_msg, "passthrough")
except Exception as e:
# 如果转换过程中出现错误，记录错误日志并返回
rospy.logerr("cv_bridge error: %s", str(e))
return
# 获取图像尺寸
h, w = color_cv.shape[:2]
# 在图像中心位置画一个红色圆圈，作为中心点标记
center = (w // 2, h // 2)
cv2.circle(color_cv, center, radius=5, color=(0, 0, 255), thickness=2)
# 示例：打印中心点处的深度值
if depth_cv is not None and depth_cv.size > 0:
center_depth = depth_cv[h//2, w//2]
if depth_cv.dtype == 'uint16' and center_depth > 0:
center_depth /= 1000.0  # 将单位从毫米转换为米
rospy.loginfo("Synced! Center depth: %.2f m", center_depth)
# 使用cv_bridge将标注后的OpenCV图像转换回ROS图像消息格式，并通过定义的发布者发布出去
annotated_image_msg = bridge.cv2_to_imgmsg(color_cv, encoding="bgr8")
annotated_image_pub.publish(annotated_image_msg)
def main():
"""
主函数，初始化节点并设置订阅者与回调函数
"""
# 初始化ROS节点
rospy.init_node('rgbd_sync_simple')
# 设置订阅者，分别订阅彩色和对齐后的深度图像
color_sub = message_filters.Subscriber('/camera/color/image_raw', Image)
depth_sub = message_filters.Subscriber('/camera/aligned_depth_to_color/image_raw', Image)
# 使用ApproximateTimeSynchronizer来同步两个不同话题的消息
ts = message_filters.ApproximateTimeSynchronizer(
[color_sub, depth_sub],
queue_size=10,
slop=0.1
)
# 注册回调函数
ts.registerCallback(image_callback)
rospy.loginfo("Waiting for RGB-D data...")
# 进入ROS的循环中，保持节点运行
rospy.spin()
if __name__ == '__main__':
try:
main()
except rospy.ROSInterruptException:
pass
7.3.5 Realsens D435补充说明
无
7.4 空循环LinkTrack UWB
7.4.1 LinkTrack UWB介绍
1. 硬件参数
2. 连接方法
3. 作用
7.4.2 LinkTrack UWB相关SDK及ROS包安装
出场设备均已安装LinkTrack UWB相关SDK及ROS包
7.4.3 LinkTrack UWB的参数配置及启动
7.4.4 LinkTrack UWB数据的应用
7.4.5 LinkTrack UWB补充说明
无
7.5 USB相机
7.5.1 USB相机介绍
1. 硬件参数
2. 连接方法
USB相机插入机载电脑的USB接口即可完成连接
在终端输入lsusb查看设备是否正常连接
lsusb
3. 作用
通过USB摄像头图像，我们可以使用opencv或深度学习算法完成图像的处理和识别，也可以进行拍照，录像等功能。
7.5.2 USB相机的使用
在将USB相机通过USB连接到机载电脑后，我们就可以使用opencv和ros来读取发布图像了。
在sensor_examples功能包scripts目录下创建代码文件usb_cam.py。
1. 设备识别与初始化
首先我们要正确找到设备的索引，并完成摄像头的初始化
以下代码实现了相关库的导入，ros节点的初始化，创建摄像头实例，指定摄像头索引和使用的视频I/O后端，并进行了摄像头是否成功打开以及能否读取到图像的判断，通过以下步骤，即可完成USB摄像头的设备识别与初始化。
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 导入ros的python接口
import rospy
# 导入opencv
import cv2
# 导入ros的Image数据类型和将opencv数据转为rosmsgs的cv_bridge库
from sensor_msgs.msg import Image
from cv_bridge import CvBridge, CvBridgeError
def main():
# 初始化ros节点
rospy.init_node('usb_cam_publish')
# 实例化转换库
bridge = CvBridge()
# 创建摄像头实例，指定摄像头索引和使用的视频I/O后端
cap = cv2.VideoCapture(0, cv2.CAP_V4L2)
# 判断摄像头是否打开
if not cap.isOpened():
rospy.logerr("Failed to open camera")
return
# 判断能否读到图像
ret, frame = cap.read()
if not ret:
rospy.logwarn("Failed to grab frame from camera")
return
rospy.loginfo(f"Camera opened successfully")
其中摄像头的索引在设备中并不固定，因此我们需要通过指令去查询索引
在终端输入ls /dev/video*指令查看USB摄像头设备
ls /dev/video*
我们可以看到有/dev/video0和/dev/video1两个索引，一般前面的才是正确的索引序号，因此，我们将代码cap = cv2.VideoCapture(0, cv2.CAP_V4L2)中的索引改为0，这样基本上就可以正确打开摄像头了。
当我们需要使用多个摄像头时，可以通过插拔摄像头并观察ls /dev/video*指令的输出变化，即可判断每一个摄像头对应的索引。
2. 查询与设置视频参数
在我们正确打开摄像头后，我们就可以通过设置参数去配置摄像头的工作模式。
首先，我们通过v4l2-ctl -d /dev/video0 --list-formats-ext指令，查看摄像头支持的图像格式，分辨率及帧率。不同的摄像头支持的格式，分辨率，帧率可能不同。其中的/dev/video0替换为自己的摄像头索引。
v4l2-ctl -d /dev/video0 --list-formats-ext
根据输出，我们可以知道该摄像头支持YUYV和MJPG两种图像格式，以及在不同图像格式下支持的分辨率以及帧率，可以发现，只有在MJPG格式下，1920×1080能达到30帧，YUYV格式下，超过640×480帧率就会下降。这是因为YUYV输出的是无损的原始图像，带宽占用高，MJPG输出的是有损的缩后的图像，带宽占用低。摄像头默认输出YUYV格式，640×480，30帧的图像，一般情况下，默认输出即可满足使用。
接着，我们通过v4l2-ctl -d /dev/video0 --list-ctrls指令，查看摄像头支持的一些控制参数。不同的摄像头支持的控制参数可能不同。其中的/dev/video0替换为自己的摄像头索引。
v4l2-ctl -d /dev/video0 --list-ctrls
根据输出，我们可以知道，该摄像头支持亮度（brightness）、对比度（contrast）、饱和度（saturation）、色调（hue）、伽马校正（gamma）、增益（gain）、锐度（sharpness）、背光补偿（backlight_compensation）、自动/手动曝光控制（exposure_auto、exposure_absolute）、曝光优先级（exposure_auto_priority）、自动/手动白平衡（white_balance_temperature_auto、white_balance_temperature）以及电源抗闪烁频率（power_line_frequency）这些控制参数的修改。以及它的最大值，最小值，步长，默认值和当前值。大部分情况下，这些参数都不需要改动，保持默认即可。
综上所述，当成功打开设备并完成初始化后，完全可以不进行任何的格式参数修改，就可以直接使用。如若需要修改格式参数，使用以下指令即可完成修改。
（1）修改图像格式（必须第一个设置），分辨率和帧率
将图像改为YUYV格式
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc('Y', 'U', 'Y', 'V'))
将图像改为MJPG格式
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc('M', 'J', 'P', 'G'))
修改分辨率
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)   # 宽
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)  # 高
修改帧率
cap.set(cv2.CAP_PROP_FPS, 30)
打印当前图像格式，分辨率和帧率
fourcc_code = int(cap.get(cv2.CAP_PROP_FOURCC))
fourcc_str = "".join([chr((fourcc_code >> 8 * i) & 0xFF) for i in range(4)])
width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
fps = cap.get(cv2.CAP_PROP_FPS)
print(f"  像素格式 (FOURCC): '{fourcc_str}'")
print(f"  分辨率           : {width} × {height}")
print(f"  帧率 (FPS)       : {fps:.2f}")
（2）修改控制参数
修改亮度并打印当前亮度
# 1. brightness（亮度）
cap.set(cv2.CAP_PROP_BRIGHTNESS, 0)          # -64 ~ 64
brightness = cap.get(cv2.CAP_PROP_BRIGHTNESS)
print(f"  Brightness (亮度)           : {brightness}")
修改对比度并打印当前对比度
# 2. contrast（对比度）
cap.set(cv2.CAP_PROP_CONTRAST, 32)           # 0 ~ 64
contrast = cap.get(cv2.CAP_PROP_CONTRAST)
print(f"  Contrast (对比度)           : {contrast}")
修改饱和度并打印当前饱和度
# 3. saturation（饱和度）
cap.set(cv2.CAP_PROP_SATURATION, 64)         # 0 ~ 128
saturation = cap.get(cv2.CAP_PROP_SATURATION)
print(f"  Saturation (饱和度)         : {saturation}")
修改色调并打印当前色调
# 4. hue（色调）
cap.set(cv2.CAP_PROP_HUE, 0)                 # -40 ~ 40
hue = cap.get(cv2.CAP_PROP_HUE)
print(f"  Hue (色调)                  : {hue}")
修改伽马并打印当前伽马
# 5. gamma（伽马）
cap.set(cv2.CAP_PROP_GAMMA, 100)             # 72 ~ 500
gamma = cap.get(cv2.CAP_PROP_GAMMA)
print(f"  Gamma (伽马)                : {gamma}")
修改增益并打印当前增益
# 6. gain（增益）
cap.set(cv2.CAP_PROP_GAIN, 0)                # 0 ~ 100
gain = cap.get(cv2.CAP_PROP_GAIN)
print(f"  Gain (增益)                 : {gain}")
修改锐度并打印当前锐度
# 7. sharpness（锐度）
cap.set(cv2.CAP_PROP_SHARPNESS, 2)           # 0 ~ 6
sharpness = cap.get(cv2.CAP_PROP_SHARPNESS)
print(f"  Sharpness (锐度)            : {sharpness}")
修改色温并打印当前色温
手动修改色温时，必须先关闭自动白平衡
# 8. white_balance_temperature_auto（自动白平衡）
cap.set(cv2.CAP_PROP_AUTO_WB, 1)             # 0=关闭, 1=开启
# 8. white_balance_temperature（手动色温）→ 必须在 auto_wb=0 后设置
cap.set(cv2.CAP_PROP_WB_TEMPERATURE, 4600)   # 2800 ~ 6500 K
auto_wb = cap.get(cv2.CAP_PROP_AUTO_WB)
wb_temp = cap.get(cv2.CAP_PROP_WB_TEMPERATURE)
print(f"  Auto White Balance          : {'ON' if auto_wb else 'OFF'}")
print(f"  White Balance Temperature   : {wb_temp} K")
修改曝光值并打印当前曝光值
手动修改曝光值时，必须先关闭自动曝光模式
# 9. exposure_auto（自动曝光模式）→ 设为 1 表示 Manual
cap.set(cv2.CAP_PROP_AUTO_EXPOSURE, 3)     # 注意：1 = Manual, 3 = Auto
# 9. exposure_absolute（手动曝光值）
cap.set(cv2.CAP_PROP_EXPOSURE, 157)          # 1 ~ 5000
auto_exposure = cap.get(cv2.CAP_PROP_AUTO_EXPOSURE)
exposure_val = cap.get(cv2.CAP_PROP_EXPOSURE)
exposure_mode = "Manual" if auto_exposure == 1 else "Auto"
print(f"  Auto Exposure Mode          : {exposure_mode} (raw={auto_exposure})")
print(f"  Exposure Value              : {exposure_val}")
3. 图像获取与实时显示
当我们成功打开摄像头完成初始化，并进行了相关参数的设置之后，我们就可以开始读取图像并查看实时图像。
通过以下代码，我们先注册了一个ros的发布者image_pub，向'/usb_cam/image_raw'话题发布Image数据类型的消息，接着我们使用rospy.Rate来限制循环的帧率，固定帧率发布图像，同时避免CPU空转。
接着我们创建一个循环，以每秒30帧的频率运行，在循环内，先通过cap.read()读取摄像头图像，接着通过ret来判断图像是否存在，若不存在则进入下一个循环，若存在，则使用bridge将从摄像头获取到的opencv图像转为ros的Image格式，再使用先前注册的发布者image_pub将转换后的图像数据ros_image发布出去，从何供其他订阅者使用以及使用rqt_image_view完成可视化。
# 创建发布者
image_pub = rospy.Publisher('/usb_cam/image_raw', Image, queue_size=1)
# 使用Rate限制帧率
rate = rospy.Rate(30)  # 30 FPS
try:
while not rospy.is_shutdown():
# 读取一帧图像
ret, frame = cap.read()
# 判断能否读到图像
if not ret:
rospy.logwarn("Failed to grab frame from camera")
continue
try:
# 将opencv图像转为ros的Image格式
ros_image=bridge.cv2_to_imgmsg(frame, encoding="bgr8")
# 将图像发布出去
image_pub.publish(ros_image)
except CvBridgeError as e:
rospy.logerr(f"CvBridge Error: {e}")
continue
# 控制循环频率
rate.sleep()
except rospy.ROSInterruptException:
rospy.loginfo("ROS interrupt received. Shutting down...")
finally:
cap.release()
rospy.loginfo("Camera released.")
至此，我们已经完成了USB摄像头的初始化，参数设置，读取图像，发布图像这几个步骤，接着我们来运行代码查看效果（完整代码参考资料包）
首先在终端启动一个roscore
roscore
接着，新开一个终端，输入rosrun sensor_examples usb_cam.py启动编写的USB摄像头读取发布程序
rosrun sensor_examples usb_cam.py
最后，新开一个终端，输入rqt_image_view，选择代码内创建的发布话题'/usb_cam/image_raw'，查看发布的实时图像。
rqt_image_view
7.5.3 USB相机的内参标定
除了简单的图像处理以外，当我们使用USB摄像头进行apriltag码识别等功能时，摄像头的畸变会极大的影响使用效果，因此，我们要对其进行畸变校正，标定内参和畸变系数。
1. 准备标定板
在进行摄像头的标定前，需要准备一块标定板，标定板可以网上购买，不过比较昂贵，也可以将资料包中的标定板文件用A4纸打印出来，固定在硬纸板上使用，不过受限于打印机的精度和粘贴时的平整度，会对标定结果产生影响，但用于一般的比赛来说基本够用。
实际成品如图所示，其中每个小格子的边长为0.02m，即2cm。
2. 调整相机和准备代码
首先在进行标定，要调整好相机的焦距（拧镜头），使成像清晰。
接着，安装用于标定的camera_calibration功能包，一般情况下，设备已提前安装好，此步可跳过。
若未安装，则在命令行输入以下指令安装，其中的noetic替换为自己的ros版本。
sudo apt install ros-noetic-camera-calibration
安装完camera_calibration功能包后，我们参考上一章节《USB相机的使用》，来编写一个摄像头图像的发布节点，用于校正，当然也可以直接使用上一章的例程。
3. 开始标定
在完成了标定板、相机和代码的准备后，我们开始进行标定。
首先在终端输入roscore启动rosmaster
roscore
接着，启动摄像头图像的发布节点，我们在这里启动上一章的例程
rosrun sensor_examples usb_cam.py
最后，启动标定节点，该标定节点有三个参数
size：标定板的内角点数量
如下图所示，每个红圈圈的地方就是一个内角点，示例图中，横向有11个内角点，纵向有8个内角点，所以参数填11x8。
注意11x8中的‘x’为小写字母‘x’，而不是数学符号‘*’。使用时根据你所使用标定板实际尺寸修改。
square：标定板中每个小格子的边长
示例中的标定板的小格子边长为2cm，也就是0.02m，所以参数填0.02，使用时根据你所使用标定板实际尺寸修改。
image：发布摄像头图像的ros话题
例程中注册的话题名为/usb_cam/image_raw，所以我们填/usb_cam/image_raw。使用时根据你实际的话题名修改。
修改完参数后，使用以下以下指令启动标定节点
rosrun camera_calibration cameracalibrator.py --size 11x8 --square 0.02 image:=/usb_cam/image_raw
启动后会出现这样的界面
拿着标定板放在相机视野内，标定板上出现彩色线条则为有效图像，反之无效，需要调整标定板位置。
依次在左右（X），上下（Y），远近（Size），倾斜（Skew）方向移动，直到标定界面上对应方位下方的进度条为绿色，移动过程尽量缓慢，避免大幅度的移动使相机成像产生拖影。为了得到一个好的标定结果，应该使得标定板尽量出现在摄像头视野的各个位置里，比如相机视野的左边，右边，上边和下边，标定板既有倾斜的，也有水平的，离得近的远的都要有，最好保证所有的进度条都是绿色满格的 。
当所有进度条都变成绿色后，CALIBRATE按钮由灰色变成深绿色，点击CALIBRATE，点击后，界面会卡住，此时不要做任何操作，直到运行标定程序的终端输出标定的结果，如下图所示。
标点完成后再将标定板放在相机视野内，右上角会出现标定结果的线性误差，通常这个值小于0.1或者显示acc都可以认为标定结果可用。
点击标定界面的SAVE按钮，标定结果保存在/tmp/calibrationdata.tar.gz这个压缩包中，CTRL+C退出程序。
我们使用cp指令将压缩包从tmp目录拿出来
cp /tmp/calibrationdata.tar.gz ~/ros_test_ws/src/sensor_examples/config/
使用tar指令解压缩文件，得到一堆标定时的图片和文件
tar -xvf calibrationdata.tar.gz
其中的ost.yaml文件就是我们标定的内参，其他的文件可以删掉。至此，内参标定结束。
4. 发布内参数据
在完成USB相机内参的标定后，我们就可以将内参通过ros话题发布出去，供有需要的节点来使用。
在sensor_examples功能包scripts目录下创建代码文件usb_info.py。
我们只需要简单的导入ros中承载内参的CameraInfo消息类型和yaml文件处理库，读取文件路径下的参数文件，将数据依次填入CameraInfo消息中，然后通过发布者发布出去即可。定义发布话题为'/usb_cam/camera_info'（完整代码参考资料包）。
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# 导入ros的python接口
import rospy
# 导入ros的CameraInfo数据类型
from sensor_msgs.msg import CameraInfo
# 导入yaml文件相关处理库
import yaml
from yaml import CLoader as Loader
def load_camera_info(yaml_file):
"""
从YAML文件加载相机标定信息
:param yaml_file: YAML文件路径
:return: sensor_msgs/CameraInfo对象
"""
with open(yaml_file, 'r') as file:
calib_data = yaml.load(file, Loader=Loader)
camera_info_msg = CameraInfo()
camera_info_msg.width = calib_data['image_width']
camera_info_msg.height = calib_data['image_height']
camera_info_msg.K = calib_data['camera_matrix']['data']
camera_info_msg.D = calib_data['distortion_coefficients']['data']
camera_info_msg.R = calib_data['rectification_matrix']['data']
camera_info_msg.P = calib_data['projection_matrix']['data']
camera_info_msg.distortion_model = calib_data['distortion_model']
return camera_info_msg
记得将参数文件路径改为自己的参数文件路径
yaml_file = '/home/orangepi/ros_test_ws/src/sensor_examples/config/ost.yaml'
完成代码的编写后，我们来看一下效果
首先在终端启动一个roscore
roscore
接着，新开一个终端，输入rosrun sensor_examples usb_info.py启动编写的USB摄像内参发布程序
rosrun sensor_examples usb_info.py
新开一个终端，输入rostopic echo /usb_cam/camera_info查看发布的消息
rostopic echo /usb_cam/camera_info
至此，USB摄像内参的发布也完成了。
7.5.4 USB相机补充说明
无